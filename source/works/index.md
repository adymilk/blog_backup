---
title: 作品与证据
date: 2018-05-14 15:49:39
sidebar: false
comments: false
---

<!--
  ===========================================================================
  这一页是整个门户的 C 位。

  为什么写在最前面：FDE（Forward Deployed Engineer）面试最核心的一轮是
  「模糊案例拆解」，权重约 30%、通过率最低。面试官要看的是「决策链」，
  不是你用了什么技术栈。

  每个案例按固定结构写：
    场景 → 模糊点 → 边界(in/out) → 关键决策(≥2 个备选+代价)
    → 交付节奏 → 结果(对比基线) → 运营与交接 → 复盘

  ⚠️ 证据分级（这是本页最重要的概念）：
    L0 声称      — "系统很稳定"，无测量方法。不能算证据，不要写
    L1 展示      — 截图、架构图、一次成功演示
    L2 可复现    — 有数据、命令、版本、预期输出，别人能重跑
                   ← 旗舰案例核心链路至少要到这一级
    L3 经使用验证 — 有真实试点用户、基线、结果与限制

  ⚠️ 没有真实客户的项目，必须明确标注「模拟企业场景」。
     调研结论明确说：可信度比规模重要，诚实标注反而加分。

  ⚠️ 每个 TODO(James) 都是需要你填的真实信息。用 `npm run check` 查看残留。
  ===========================================================================
-->

## 一句话说明

<!-- TODO(James): 用一句话说清这些项目的共性。
     参考方向（据实调整）：把工业现场的模糊问题做成能跑的系统。 -->

---

## 案例 1 · Minitab COM 桥接服务

{% note info %}
**核心约束**：Minitab 只开放 C# 的 COM 组件接口，而 COM 组件**无法被 Web 程序直接调用**。
{% endnote %}

**技术事实**（来自本站技术笔记，已成事实）：

- 写了一个 Windows 服务，开机自启
- 服务持续轮询一个 HTTP 接口取任务；接口参数含**分析类型、子组大小、判异原则**等变量
- 服务收到参数后调用 Minitab COM 组件做统计分析，生成图表与文字说明
- 前端调用 Web 接口时必须传**唯一标识 `OrderID`**（实际是向数据库写一条记录），
  之后前端循环查询该 `OrderID` 的结果

**证据等级**：L1 → <!-- TODO(James): 目标至少 L2 -->

### 决策链

<!-- TODO(James): 以下 8 项是本案例最重要的部分，也是面试官唯一会追问的部分。
     已经给了提示，但事实需要你填 —— 我不知道真实情况，不会替你编。 -->

- **场景** <!-- TODO(James): 客户是谁（行业即可，如「某汽车零部件厂」）、
  业务背景、谁在用这个系统。如果是内部项目就写内部。 -->
- **模糊点** <!-- TODO(James): 初始需求里没说清的地方。例如：
  客户只说「想在网上看到 SPC 图」，但没说刷新频率、没说并发用户数、
  没说 Minitab 授权是单机的、没说数据从哪来。 -->
- **边界（in / out scope）** <!-- TODO(James): 明确划出去的部分。
  例：「不做实时流式，改 N 秒轮询」「不做多租户」。
  ⚠️ 划范围本身就是考点 —— FDE 面试最高频的秒拒信号是
  「在 scoping 之前就跳到解决方案」。 -->
- **关键决策** <!-- TODO(James): 至少两个备选方案 + 各自代价。
  例：备选 A 用 C# 重写分析逻辑（代价：复现不了 Minitab 的判异规则）；
  备选 B 用 COM 桥接（代价：受限于 Windows + Minitab 单机授权）。
  选了 B 的理由是 ___。这个「为什么」是面试官最想听的。 -->
- **交付节奏** <!-- TODO(James): 从开工到上线分了几个阶段、每阶段交付什么。 -->
- **结果（对比基线）** <!-- TODO(James): ⚠️ 这一项现在完全缺失，而它最重要。
  原技术笔记里只有技术描述，没有「业务改变了什么」。例：
  原来 ___ 分钟/次 → 现在 ___ 秒/次；原来需要 ___ 人天 → 现在 ___。
  如果确实无法量化，就写「无法量化」并说明为什么 —— 不要编数字。 -->
- **运营与交接** <!-- TODO(James): 谁在维护、怎么培训用户、有没有文档、
  服务挂了怎么办。原笔记提到「开机后自动运行」，但没提失败重试、
  日志、告警 —— 补上这些描述，能立刻让「生产与可靠性」这一维有证据。 -->
- **复盘** <!-- TODO(James): 如果重来会怎么改。这一节体现成长性。 -->

### 可复现证据

<!-- TODO(James): 这是把案例从 L1 抬到 L2 的关键，也是本页最该补的东西。
     最低要求四件套：数据（脱敏样本）+ 命令 + 版本 + 预期输出。
     例：
       - 环境：Windows Server 20XX + Minitab XX + .NET Framework X.X
       - 数据：脱敏样本（放到 source/uploads/ 下）
       - 命令：curl -X POST .../api/analyze -d '{"orderId":"...","subgroupSize":5}'
       - 预期输出：控制图截图 + 判异说明
       - 源码：能开源就开源；不能开源就说明原因（如客户合同限制） -->

→ 相关技术笔记：[数据科学](/2020/07/14/数据科学/)

---

## 案例 2 · Minitab 二次开发实时生成控制图

{% note success %}
有一个**可演示的交付物**（视频）—— 这在个人站上很稀缺，是本案例最大的资产。
{% endnote %}

**演示**：

![](https://techmax.com.cn/uploadfile/2021/0226/20210226105352646.jpg)

<video src='https://techmax.com.cn/public/spc.mp4' type='video/mp4' controls width='100%'></video>

<!-- ⚠️ 注意：上面两个资源都外链 techmax.com.cn。
     如果那台服务器下线或路径变更，页面会开天窗。
     建议把图片转存到 source/uploads/，视频如果太大就转存到对象存储或 B 站，
     然后替换上面的链接。 -->

**证据等级**：L1（有演示）→ <!-- TODO(James): 目标至少 L2 -->

### 决策链

<!-- TODO(James): 同案例 1 的 8 项结构。本案例与案例 1 有重叠，
     写的时候要突出「增量」：相比案例 1，这次多解决了什么问题？
     是实时性？是图表种类？是并发？还是判异规则的准确性？ -->

- **场景** <!-- TODO(James) -->
- **模糊点** <!-- TODO(James) -->
- **边界（in / out scope）** <!-- TODO(James) -->
- **关键决策** <!-- TODO(James) -->
- **交付节奏** <!-- TODO(James) -->
- **结果（对比基线）** <!-- TODO(James) -->
- **运营与交接** <!-- TODO(James) -->
- **复盘** <!-- TODO(James) -->

→ 相关技术笔记：[Minitab 二次开发实时生成控制图](/2021/06/29/20210629-1/)

---

## 案例 3 · <!-- TODO(James): LLM / GenAI 方向 -->

{% note warning %}
**这是当前最大的缺口，也是优先级最高的待补项。**

FDE 岗位的一手 JD（Anthropic / OpenAI）硬性要求
「LLM / GenAI 生产经验的**可指认证据**」：提示工程、agent 开发、
**评测框架**、RAG、MCP 服务器、规模化部署。

本案例现在留空。**不要写「正在学习」** —— 那反而暴露缺口。做完了再放上来。
{% endnote %}

<!-- 建议路径（按投入从低到高，任选其一先把「证据」做出来）：

  1. 【性价比最高】给你已有的 Minitab / SPC 场景接一层 LLM：
     自然语言提问 → 生成 Minitab 分析参数 → 返回结果解读。
     关键是要有【评测集】，不是只有一个 demo。

  2. 做一个 MCP 服务器，把 Minitab 分析能力暴露成 MCP tool。
     这直接命中 Anthropic JD 的关键词，且很适合挂在作品页上。

  3. 【最能差异化】建评测框架：给自己的 LLM 功能建 20–50 条 golden set，
     报告准确率与回归情况。
     「评测」是 FDE 六维里最稀缺的能力 —— 大多数候选人只会调 API，
     不会证明自己的系统真的有效。这一步的性价比最高。

  写完后请同时更新 source/_data/profile.yml 的 projects 段，
  并把 evidence_level 标成 L2（可复现）。 -->

---

## 其他开源 / 作品

<!-- TODO(James): 只放「证据完整」的项目，宁少勿滥。真实项目优先，star 数不重要。

     从你的 GitHub 看到这些仓库（可作素材，但目前都还比较薄）：
       - adymilk/vue-laravel-websocket-demo
       - adymilk/plink-html-template
       - adymilk/python_spider

     更该展示的形态是：一个有 README、能按一条命令跑起来、
     有测试、有数据样例的项目仓库。 -->

---

## 关于证据的说明

本站所有案例都标注了**证据等级**（L0–L3）。我倾向于把等级标得保守一些 ——
一个标成 L2 且真的能重跑的案例，比三个标成 L3 但经不起追问的案例更有说服力。
