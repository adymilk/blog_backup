---
title: 王恒 · 简历
date: 2018-06-10 18:00:21
layout: resume
type: resume
sidebar: false
comments: false
---

<!--
  ===========================================================================
  本页的「头部卡片 / 核心能力 / 技能关键词」由 source/_data/profile.yml 渲染。
  这个文件只写「叙事」部分 —— 也就是 case study 的决策链。

  为什么这么分：
    · 结构化的短字段（联系方式、技能等级、关键词）放 YAML —— HR 机器筛选
      和多个页面复用都需要它
    · 长叙事（案例、工作经历）放 markdown —— YAML 的块标量写多段带列表的
      文本会失控

  ⚠️ 证据分级（和 /works/ 页一致）：
    L0 声称 < L1 展示 < L2 可复现 < L3 经使用验证
    旗舰案例核心链路至少到 L2 —— 别人能按你写的「数据+命令+版本+预期输出」重跑
    没有真实客户的项目必须标注「模拟企业场景」；诚实标注比夸大更可信

  ⚠️ 所有 TODO(James) 需要你填。用 `npm run check` 查看残留数量。
  ===========================================================================
-->

{% note warning %}
本页正在重构中。带 TODO 标记的部分尚未填写，**目前还不适合投递**。
{% endnote %}

## 一句话定位

<!-- TODO(James): 一句话说清「我是谁 + 我解决什么问题 + 给谁」。
     这是整页最重要的一句话，HR 只会读这一句。

     调研给出的定位方向（据实调整，不要照抄）：
     「懂工业现场与统计数据的全栈工程师，能亲手把 LLM 落地到制造业工作流。」

     写完请同步到 source/_data/profile.yml 的 identity.one_liner，
     两处必须一致。 -->

## 求职意向

<!-- TODO(James): 目标岗位 / 目标城市 / 到岗时间 / 是否接受出差。
     上面头部卡片已经从 profile.yml 读了一部分，这里展开写理由。

     ⚠️ 建议主动写明「可接受驻场 / 出差」——
     FDE 岗位普遍要求驻客户现场，主动写是加分项而不是减分项。 -->

## 为什么是 FDE

<!-- TODO(James): 这是 FDE 面试第一轮就会问的问题（"为什么是 FDE 而不是普通 SWE？"）。
     建议从「你过去做过的什么事让你发现自己更适合这个角色」切入，
     而不是从「我看好这个方向」切入 —— 前者是证据，后者是声称。

     可以结合你在 Minitab 二次开发里「既要写代码又要理解客户的质量管理流程」
     这类经历来写。 -->

---

## 代表案例

<!--
  每个案例按「决策链」写，这是 FDE 面试的核心评分点。
  /works/ 页里已经有同样结构的骨架，这里写的是「简历视角」的浓缩版 ——
  更短、更聚焦于你个人的决策和贡献（用「我」而不是「我们」）。

  ⚠️ 至少两个备选方案 + 各自代价。这是决策链里唯一不能省的部分。
-->

### 案例 1 · Minitab COM 桥接服务

- **约束**：Minitab 只开放 C# COM 接口，无法被 Web 程序调用
- **我的方案**：Windows 服务 + HTTP 轮询 + `OrderID` 异步取结果
- **关键取舍**：<!-- TODO(James): 至少两个备选 + 代价。
  例：为什么不用 C# 重写分析逻辑（代价：复现不了 Minitab 的判异规则）？
  为什么是轮询而不是 WebSocket / 消息队列？ -->
- **结果**：<!-- TODO(James): 对比基线。原技术笔记里只有技术描述，
  缺「业务改变了什么」。原来 ___ → 现在 ___。
  量化不了就写「无法量化」并说明为什么，不要编数字。 -->
- **证据等级**：L1 → <!-- TODO(James): 目标 L2，见 /works/ 页 -->

### 案例 2 · Minitab 二次开发实时生成控制图

- **交付物**：可演示的 SPC 实时控制图（有视频）
- **我的方案**：<!-- TODO(James) -->
- **关键取舍**：<!-- TODO(James) -->
- **结果**：<!-- TODO(James) -->
- **证据等级**：L1 → <!-- TODO(James) -->

### 案例 3 · LLM / GenAI 方向

{% note warning %}
**当前最大的缺口。** Anthropic / OpenAI 的一手 JD 硬性要求
「LLM / GenAI 生产经验的可指认证据」。

建议路径见 [作品与证据页](/works/) 的案例 3。做完了再把这条填上 ——
不要写「正在学习」。
{% endnote %}

---

## 工作经历

<!-- TODO(James): 公司 / 职位 / 起止时间 / 一段话职责 + 一条最硬的量化结果。

     ⚠️ 必须与 source/about/index.md 和 source/_data/profile.yml 三者一致。
     这三个地方曾经对同一段履历给出互相矛盾的版本
     （学历、城市、公司、工作年限都不同）——
     同一份履历在同一个站上出现两个版本，比没写更糟。 -->

### <!-- TODO(James): 公司名 · 职位 · 起止时间 -->

<!-- TODO(James): 职责范围 + 量化结果 -->

### <!-- TODO(James): 公司名 · 职位 · 起止时间 -->

<!-- TODO(James) -->

---

## 教育背景

<!-- TODO(James): 学校 / 专业 / 学历 / 起止时间。
     与 about 页统一 —— 目前 about 写的是 AnHui University Of Science And
     Technology，请确认后再改。 -->

---

## 开源与作品

<!-- TODO(James): 指向 /works/ 页与 GitHub。

     已有仓库（可作素材，但目前都还比较薄）：
       - adymilk/vue-laravel-websocket-demo
       - adymilk/plink-html-template
       - adymilk/python_spider

     该放的是「有 README、能一条命令跑起来、有测试、有数据样例」的项目，
     而不是 star 数。 -->

---

## 关于本站

这个站是我的技术笔记，也是我的在线简历。技术笔记从 2015 年写到现在，
记录了从 PHP 到 Laravel、Go、React，再到工业数据分析这条路径。

案例都标注了**证据等级**，欢迎验证。
